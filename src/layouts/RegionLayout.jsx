import { useParams } from "react-router-dom";

function RegionPage() {
  const { id } = useParams();

  return (
    <div className="region-page">
      





    </div>
  );
}

export default RegionPage;
